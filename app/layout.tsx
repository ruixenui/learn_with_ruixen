import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-sans",
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-mono",
});

export const metadata: Metadata = {
  title: "Learn Ruixen UI - Design Engineering Components",
  description:
    "Learn the art of micro-interactions and design refinement. Each lesson focuses on the subtle details that transform ordinary UI elements into refined, tactile experiences.",
  keywords: [
    "design engineering",
    "UI components",
    "micro-interactions",
    "React",
    "Next.js",
    "Tailwind CSS",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${jetbrainsMono.variable}`}>
      <body className="font-sans">{children}</body>
    </html>
  );
}
